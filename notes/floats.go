package main

import (
	"fmt"
)

func main() {
  // Floats are numbers with decimal places
  // Only use floats when necessary
  // Uses more memory(?) than integers
  exampleFloat := 0.5

  fmt.Println(exampleFloat)
}
